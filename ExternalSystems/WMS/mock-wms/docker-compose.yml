version: '3'

services:
  mock-wms:
    build: .
    ports:
      - "5000:5000"  # TCP server
      - "5001:5001"  # HTTP API
    environment:
      - RUST_LOG=info
      - WMS_PORT=5000
    restart: unless-stopped
